name: Build and Deploy NextJS app

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: actions/checkout@v3
        with: 
          node-version: 18.13.0
          cache: npm
      
      - name: Build
        run: |
          cd website-portfolio-code
          npm i
          npm run build
          npm run export
